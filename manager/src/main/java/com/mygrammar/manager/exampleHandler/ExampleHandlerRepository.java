package com.mygrammar.manager.exampleHandler;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ExampleHandlerRepository extends JpaRepository<ExampleHandler, Integer> {
}
